// Define num1 and num2
let num1 = 31;
let num2 = 2;

// Multiply num1 and num2
let multiply = num1 * num2; 

// Generate a random integer greater than 0
let random = Math.floor(Math.random() * 100) + 1;

// Define num3 and num4
let num3 = 16;
let num4 = 6;

// Calculate remainder of num3 divided by num4
let mod = num3 % num4; 

// Find the highest number in a set
let max = Math.max(1, 5, 10, 15, 20); 

// Export the variables if needed for testing
module.exports = { multiply, random, mod, max };
